section

  section
    h1
      code position

  section
    pre(style="font-size:3vw;").
      position: relative
                absolute
                fixed
                sticky
    pre.fragment(style="font-size:3vw; font-weight:bold; color:yellow;").
      &nbsp;         static
    aside.notes(data-markdown).
      **QUIZ**: Which of these is the default value?

  section
    h4
      code postion:relative
    iframe(data-src="examples/relative.html" data-preload="false" style="width:100%; height:50vh; box-shadow:5px 5px 5px rgba(0,0,0,0.5)")


  section
    h4
      code postion:fixed
    iframe(data-src="examples/fixed.html" data-preload="false" style="width:100%; height:50vh; box-shadow:5px 5px 5px rgba(0,0,0,0.5)")

  section
    h4
      code postion:sticky
    iframe(data-src="examples/sticky.html" data-preload="false" style="width:100%; height:50vh; box-shadow:5px 5px 5px rgba(0,0,0,0.5)")
  
  section
    h4
      code postion:static
    iframe(data-src="examples/static.html" data-preload="false" style="width:100%; height:50vh; box-shadow:5px 5px 5px rgba(0,0,0,0.5)")
    

  section
    h4 all five
    iframe(data-src="examples/all-5.html" data-preload="false" style="width:100%; height:50vh; box-shadow:5px 5px 5px rgba(0,0,0,0.5)")

  section
    dl(style="width:100%;")
      dt fixed
      dd stapled to a fixed position on your screen
    dl(style="width:100%;")
      dt absolute
      dd stapled to a position in the nearest restictive parent
    dl(style="width:100%;")
      dt sticky
      dd
        i NEW(ish)!
        |  Toggles between 
        b relative
        |  and 
        b fixed
        | , depending.
    dl(style="width:100%;")
      dt relative
      dd Follows page flow & provides a restrictive parent
    dl(style="width:100%;")
      dt static
      dd same as relative, but less helpful

  section
    table
      thead
        tr
          th 
          th relative
          th static
      tbody
        tr
          td z-index
          td ✔️
          td 🚫
        tr
          td top, bottom, left, right
          td ✔️
          td 🚫
        tr
          td holds absolute
          td ✔️
          td 🚫


  section
    h3 Okay, so why does 
      span(style="font-family:monospace;") position:static
      |  exist?

  section
    pre.huge
      code.css.
        * { position:relative; }

  section
    span My CodePen article on the subject: 
      a(href="https://codepen.io/ahoylemon/post/z-index-is-weird") Z-Index Is Weird